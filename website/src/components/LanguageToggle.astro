---
const pathname = Astro.url.pathname;
const normalizedPath = pathname.endsWith('/') && pathname !== '/' ? pathname.slice(0, -1) : pathname;
const isJa = normalizedPath === '/ja' || normalizedPath.startsWith('/ja/');

let enHref, jaHref;
if (isJa) {
  enHref = normalizedPath.replace(/^\/ja/, '') || '/';
  jaHref = normalizedPath;
} else {
  enHref = normalizedPath;
  jaHref = normalizedPath === '/' ? '/ja' : '/ja' + normalizedPath;
}
---

<div class="lang-toggle">
  <a href={enHref} class:list={['lang-btn', { active: !isJa }]} aria-label="English">EN</a>
  <span class="sep">/</span>
  <a href={jaHref} class:list={['lang-btn', { active: isJa }]} aria-label="日本語">JA</a>
</div>

<style>
  .lang-toggle {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 0.05em;
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(12px);
    padding: 0.35rem 0.75rem;
    border-radius: 100px;
    border: 1px solid rgba(34, 211, 238, 0.12);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
  }

  .lang-toggle:hover {
    border-color: rgba(34, 211, 238, 0.35);
    background: rgba(34, 211, 238, 0.05);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  .lang-btn {
    color: #4a7fa0;
    text-decoration: none !important;
    transition: all 0.2s ease;
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    -webkit-tap-highlight-color: transparent;
  }

  .lang-btn.active {
    color: #22d3ee;
    text-shadow: 0 0 12px rgba(34, 211, 238, 0.5);
  }

  .lang-btn:hover:not(.active) {
    color: #e0f9fd;
  }

  .sep {
    color: rgba(34, 211, 238, 0.25);
    user-select: none;
    font-size: 0.75rem;
    font-weight: 400;
  }

  :global(.sl-header .lang-toggle) {
    margin-right: 0.5rem;
  }

  @media (max-width: 768px) {
    .lang-toggle {
      padding: 0.4rem 0.8rem;
    }
    .lang-btn {
      min-height: 32px;
    }
  }
</style>
